pipeline {
  agent any
  stages {
    stage('') {
      steps {
        git(branch: 'master', url: 'https://github.com/PrateekParekh/moleculer-jenkins', changelog: true, poll: true)
      }
    }
  }
}